<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="IoTHubDevice.Views.DevicesView"
             Background="Black">

  <DockPanel Width="800" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
  
    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
      <Button HorizontalAlignment="Center" VerticalAlignment="Top" Width="160" Height="50" Click="FindButtonClick">Add new device</Button>
        
      <ItemsControl Items="{Binding DeviceList}" Margin="0, 30, 0, 0">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            
            <StackPanel Orientation="Vertical">
              <TextBlock Text="{Binding Name}" Foreground="White" />
              <TextBlock Text="{Binding MACAddress}" Foreground="White" />
              <TextBlock Text="{Binding Path}" Foreground="White" />
            </StackPanel>
            
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
      
  </DockPanel>
    
</UserControl>
